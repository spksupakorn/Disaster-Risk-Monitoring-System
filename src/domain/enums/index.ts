export enum DisasterType {
  FLOOD = 'flood',
  EARTHQUAKE = 'earthquake',
  WILDFIRE = 'wildfire',
}

export enum RiskLevel {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}

export enum AlertChannel {
  SMS = 'sms',
  EMAIL = 'email',
}
